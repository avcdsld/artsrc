import Box from '@mui/material/Box';
import Divider from '@mui/material/Divider';

const Home = () => {
  return (
    <Box sx={{ paddingTop: 2 }}>
      art/src
      <Divider sx={{ margin: '32px 0' }} />
    </Box>
  );
};

export default Home;
